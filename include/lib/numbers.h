#include <types.h>

extern int is_char_digit(char c);
extern int itoa(int value, char* str, int base);
extern int ltoa(int64_t value, char* str, int base);
extern int ultoa(uint64_t value, char* str, int base);
